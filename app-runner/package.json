{
  "name": "app-runner",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^4.0.1"    
  },

  "scripts": {
    "client": "cd client && HURRA_HOSTNAME=$HURRA_HOSTNAME npm run-script start",
    "server": "cd server && npm run-script server",
    "install_client_deps": "cd client && npm install",
    "install_server_deps": "cd server && npm install",
    "install_deps": "concurrently \"npm run-script install_client_deps\" \"npm run-script install_server_deps\"",
    "startRunner": "echo \"HURRA=$HURRA_HOSTNAME\" && concurrently --kill-others-on-fail \"npm run-script server\" \"HURRA_HOSTNAME=$HURRA_HOSTNAME npm run-script client\""
  }

}

